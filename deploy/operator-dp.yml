apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipam-operator
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ipam-operator
  template:
    metadata:
      labels:
        app: ipam-operator
    spec:
      tolerations:
      - effect: NoSchedule
        key: node-role.kubernetes.io/master
      serviceAccountName: ipam-operator
      containers:
      - name: operator
        image: inwinstack/ipam-operator:v0.1.0
        args:
        - --v=2
        - --logtostderr=true